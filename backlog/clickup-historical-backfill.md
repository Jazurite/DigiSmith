# Historical backfill into ClickUp

Once ClickUp is DigiSmith's task-tracking system for new work (decided
2026-09-24), the ~55 existing `backlog/*.md` files and ~40 already-shipped
map letters in `MEMORY.md` remain untouched, tracked the old way. A bulk
import of that history into ClickUp is explicitly deferred — low confidence
it's ever worth doing, since the old entries are frozen historical record
either way.

Revisit only if a real need for querying/reporting on historical items from
within ClickUp itself comes up. Depends on the ClickUp connector package
shipping first.
