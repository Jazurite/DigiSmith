# PR Descriptions

Short and plain — no `## Summary` / `## Test plan` headers or bulleted checklists.

- If the repo has its own `.github/pull_request_template.md`, use that
  instead of inventing a format

**Why:** a templated, headered description reads as AI-generated — same
incident as commit-style (PR emma-sleep/shopify-hub#63).
